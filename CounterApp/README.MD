We saw applicaton of 
-> document.querySelector - It's usd to select and return first element in the document.
-> addEventListener - It's used to add event handler to the  HTML element.
-> textContent -  It's used to get visible text on the web.

