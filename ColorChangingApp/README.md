We saw applicaton of 
-> addEventListener - It's used to add event handler to the  HTML element.
-> math.random() -  It's used to generate random value between 0 (inclusive) and 1 (exclusive).
-> Array - It's used to store multiple values.

